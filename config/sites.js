var env = 'stv.tv';

var sites =  [
        "player." + env,
        "shows." + env,
        "competitions." + env,
        "news." + env,
        "stv.tv",
        "weather." + env
    ];

module.exports = sites;